[tox]
envlist = py{36,37},pypy3

[testenv]
deps =
    readme_renderer
    flake8
    pytest
    dateparser
commands =
    python setup.py check -m -r -s
    pytest tests
[flake8]
exclude = .tox,*.egg,build,data
select = E,W,F
